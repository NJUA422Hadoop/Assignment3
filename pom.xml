<!--
    pom.xml
-->
<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>NJUA422Hadoop</groupId>
    <artifactId>InvertedIndex</artifactId>
    <version>0.1</version>
    <name>InvertedIndexAssignment</name>
    <description>
        大数据倒排索引
    </description>
    <!--
        properties
    -->
    <properties>
        <mainEntry>InvertedIndexAssignment</mainEntry>
        <jdkVersion>1.8</jdkVersion>
        <hadoopVersion>2.7.1</hadoopVersion>
        <junitEngineVersion>5.1.1</junitEngineVersion>
        <junitProviderVersion>1.1.1</junitProviderVersion>
        <surefireVersion>2.19.1</surefireVersion>
        <mavenJarPluginVersion>3.1.0</mavenJarPluginVersion>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${jdkVersion}</maven.compiler.source>
        <maven.compiler.target>${jdkVersion}</maven.compiler.target>
    </properties>
    <!--
        dependencies
    -->
    <dependencies>
        <!--junit-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junitEngineVersion}</version>
            <scope>test</scope>
        </dependency>
        <!--common-->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <version>${hadoopVersion}</version>
        </dependency>
        <!--hdfs-->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-hdfs</artifactId>
            <version>${hadoopVersion}</version>
        </dependency>
        <!--mapreduce client core-->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-mapreduce-client-core</artifactId>
            <version>${hadoopVersion}</version>
        </dependency>
        <!--clent-->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-client</artifactId>
            <version>${hadoopVersion}</version>
        </dependency>
    </dependencies>
    <!--plugins-->
    <build>
        <plugins>
        <!-- 打包插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${mavenJarPluginVersion}</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${mainEntry}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!--junit-->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefireVersion}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.junit.platform</groupId>
                        <artifactId>junit-platform-surefire-provider</artifactId>
                        <version>${junitProviderVersion}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.junit.jupiter</groupId>
                        <artifactId>junit-jupiter-engine</artifactId>
                        <version>${junitEngineVersion}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
</project>