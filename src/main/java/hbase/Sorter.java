package hbase;

import java.io.IOException;

import org.apache.hadoop.conf.Configuration;

import tools.Runner;

public class Sorter extends Runner {
  Sorter(Configuration conf) throws IOException {
    super(conf);
  }

  @Override
  protected void run() throws IOException {
    ;
  }
}